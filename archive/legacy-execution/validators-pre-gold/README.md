# Pre-Gold validator archive

These scripts validated the retired pre-Gold execution architecture and referenced removed paths such as `state/unit/`, `state/force/`, `state/force-pool/`, `state/reg/`, and `game/data/runtime/`. They are preserved only as migration evidence. The authoritative release gate is `python tools/run_gold_suite.py`.
