# Sword Runtime Contracts

The production runtime is semantic-command driven. This directory contains only the current repository authority map. Retired direct-edit routers and old process/unit contracts are archived under `archive/legacy-execution/runtime-contracts/` and are non-authoritative.
