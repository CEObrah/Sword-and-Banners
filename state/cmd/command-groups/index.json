{
  "schema": "command-group-index.v1",
  "authority": false,
  "count": 0,
  "path_template": "state/cmd/command-groups/{command_group_id}.json",
  "rule": "Command groups are mutable command-only hierarchy nodes. They own no manpower. The index is derived and rebuilt after command-group writes."
}
